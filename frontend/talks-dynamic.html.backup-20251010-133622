<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>NeoSharX Talks - Podcast Hub</title>
    <link crossorigin="" href="https://fonts.gstatic.com/" rel="preconnect" />
    <link
      href="https://fonts.googleapis.com/css2?family=Newsreader:wght@400;500;700;800&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <script>
      tailwind.config = {
        darkMode: "class",
        theme: {
          extend: {
            colors: {
              primary: "#067ff9",
              "background-light": "#f5f7f8",
              "background-dark": "#0f1923",
              "content-light": "#111418",
              "content-dark": "#f5f7f8",
              "subtle-light": "#5f758c",
              "subtle-dark": "#a0aec0",
            },
            fontFamily: {
              display: ["Newsreader"],
            },
            borderRadius: {
              DEFAULT: "0.5rem",
              lg: "0.75rem",
              xl: "1rem",
              full: "9999px",
            },
          },
        },
      };
    </script>
    <style>
      .material-symbols-outlined {
        font-variation-settings: "FILL" 0, "wght" 400, "GRAD" 0, "opsz" 24;
      }
      .loading-spinner {
        border: 4px solid #f3f3f3;
        border-top: 4px solid #067ff9;
        border-radius: 50%;
        width: 50px;
        height: 50px;
        animation: spin 1s linear infinite;
      }
      @keyframes spin {
        0% {
          transform: rotate(0deg);
        }
        100% {
          transform: rotate(360deg);
        }
      }
    </style>
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined"
      rel="stylesheet"
    />
  </head>
  <body
    class="bg-background-light dark:bg-background-dark font-display text-content-light dark:text-content-dark"
  >
    <div class="relative flex flex-col min-h-screen w-full">
      <div id="nav-container"></div>

      <main class="flex-grow container mx-auto px-4 sm:px-6 lg:px-8 py-4">
        <div class="max-w-4xl mx-auto">
          <div class="text-center mb-8">
            <h1 class="text-5xl font-extrabold tracking-tighter mb-4">
              NeoSharX Talks
            </h1>
            <p
              class="text-lg text-subtle-light dark:text-subtle-dark max-w-2xl mx-auto"
            >
              Conversations that inspire innovation. Join industry leaders and
              innovators as they share insights on technology, entrepreneurship,
              and the future.
            </p>
          </div>

          <!-- Filters -->
          <div class="flex flex-wrap items-center justify-center gap-4 mb-12">
            <select
              id="sortFilter"
              class="flex items-center gap-2 px-4 py-2 text-sm font-medium bg-background-light dark:bg-background-dark border border-subtle-light/20 dark:border-subtle-dark/20 rounded-full hover:bg-primary/10 transition-colors cursor-pointer"
            >
              <option value="recent">Sort by: Recent</option>
              <option value="episode">Episode Number</option>
              <option value="duration">Duration</option>
            </select>
          </div>

          <!-- Loading State -->
          <div id="loadingState" class="text-center py-12">
            <div class="loading-spinner mx-auto mb-4"></div>
            <p class="text-subtle-light dark:text-subtle-dark">
              Loading episodes...
            </p>
          </div>

          <!-- Error State -->
          <div id="errorState" class="hidden text-center py-12">
            <p class="text-red-500 text-lg mb-4">Failed to load episodes</p>
            <button
              onclick="location.reload()"
              class="bg-primary text-white font-bold px-6 py-3 rounded-lg hover:bg-primary/90 transition-colors"
            >
              Retry
            </button>
          </div>

          <!-- Featured Episode -->
          <div id="featuredEpisode" class="mb-16 hidden">
            <!-- Featured episode will be loaded here -->
          </div>
        </div>

        <!-- More Episodes Section -->
        <h2 class="text-3xl font-bold tracking-tight text-center mb-10">
          More Episodes
        </h2>
        <div
          id="moreEpisodes"
          class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8"
        >
          <!-- Episodes will be loaded here -->
        </div>

        <!-- Empty State -->
        <div id="emptyState" class="hidden text-center py-12">
          <p class="text-xl text-subtle-light dark:text-subtle-dark">
            No episodes found
          </p>
          <p class="text-subtle-light dark:text-subtle-dark mt-2">
            Check back soon for new episodes
          </p>
        </div>
      </main>

      <footer
        class="bg-background-light dark:bg-background-dark border-t border-subtle-light/20 dark:border-subtle-dark/20"
      >
        <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
          <div
            class="flex flex-col md:flex-row justify-between items-center gap-6"
          >
            <div class="flex items-center gap-2 text-primary">
              <svg
                class="h-6 w-6"
                fill="none"
                viewBox="0 0 48 48"
                xmlns="http://www.w3.org/2000/svg"
              >
                <path
                  d="M4 42.4379C4 42.4379 14.0962 36.0744 24 41.1692C35.0664 46.8624 44 42.2078 44 42.2078L44 7.01134C44 7.01134 35.068 11.6577 24.0031 5.96913C14.0971 0.876274 4 7.27094 4 7.27094L4 42.4379Z"
                  fill="currentColor"
                ></path>
              </svg>
              <h2 class="text-lg font-bold">NeoSharX</h2>
            </div>
            <div
              class="flex flex-wrap items-center justify-center gap-x-6 gap-y-2"
            >
              <a
                class="text-sm text-subtle-light dark:text-subtle-dark hover:text-primary transition-colors"
                href="#"
                >About</a
              >
              <a
                class="text-sm text-subtle-light dark:text-subtle-dark hover:text-primary transition-colors"
                href="#"
                >Contact</a
              >
              <a
                class="text-sm text-subtle-light dark:text-subtle-dark hover:text-primary transition-colors"
                href="#"
                >Terms of Service</a
              >
              <a
                class="text-sm text-subtle-light dark:text-subtle-dark hover:text-primary transition-colors"
                href="#"
                >Privacy Policy</a
              >
            </div>
            <p class="text-sm text-subtle-light dark:text-subtle-dark">
              © 2023 NeoSharX. All rights reserved.
            </p>
          </div>
        </div>
      </footer>
    </div>

    <script>
      const API_BASE_URL = "http://localhost:8000/api/auth";
      let allEpisodes = [];
      let filteredEpisodes = [];

      async function loadEpisodes() {
        const loadingState = document.getElementById("loadingState");
        const errorState = document.getElementById("errorState");

        try {
          console.log(
            "Fetching episodes from:",
            `${API_BASE_URL}/talk-episodes/`
          );
          const response = await fetch(`${API_BASE_URL}/talk-episodes/`);
          console.log("Response status:", response.status);

          if (!response.ok) throw new Error("Failed to fetch episodes");

          const data = await response.json();
          console.log("Received data:", data);

          allEpisodes = data.episodes || data;
          console.log("Total episodes:", allEpisodes.length);

          filteredEpisodes = [...allEpisodes];

          loadingState.classList.add("hidden");
          renderEpisodes();
        } catch (error) {
          console.error("Error loading episodes:", error);
          loadingState.classList.add("hidden");
          errorState.classList.remove("hidden");
        }
      }

      function renderEpisodes() {
        const featuredContainer = document.getElementById("featuredEpisode");
        const moreEpisodesContainer = document.getElementById("moreEpisodes");
        const emptyState = document.getElementById("emptyState");

        console.log(
          "Rendering episodes. Total filtered:",
          filteredEpisodes.length
        );

        if (filteredEpisodes.length === 0) {
          console.log("No episodes to display - showing empty state");
          featuredContainer.classList.add("hidden");
          moreEpisodesContainer.innerHTML = "";
          emptyState.classList.remove("hidden");
          return;
        }

        emptyState.classList.add("hidden");

        // Render featured episode (latest one)
        const featured = filteredEpisodes[0];
        if (featured) {
          console.log("Rendering featured episode:", featured.title);
          featuredContainer.innerHTML = renderFeaturedEpisode(featured);
          featuredContainer.classList.remove("hidden");
        }

        // Render more episodes (exclude featured)
        const moreEpisodes = filteredEpisodes.slice(1);
        console.log("Rendering additional episodes:", moreEpisodes.length);
        moreEpisodesContainer.innerHTML = moreEpisodes
          .map((episode) => renderEpisodeCard(episode))
          .join("");
      }

      function renderFeaturedEpisode(episode) {
        // Extract YouTube thumbnail from URL
        let thumbnailUrl = episode.thumbnail_url;
        if (!thumbnailUrl && episode.youtube_url) {
          const videoId = extractYouTubeID(episode.youtube_url);
          if (videoId) {
            thumbnailUrl = `https://img.youtube.com/vi/${videoId}/maxresdefault.jpg`;
          }
        }

        return `
          <a href="talks-detail.html?slug=${episode.slug}" class="block group">
            <div class="relative overflow-hidden rounded-xl bg-background-dark dark:bg-background-light h-[400px]">
              <div class="absolute inset-0 bg-cover bg-center transition-transform duration-500 group-hover:scale-105" 
                   style="background-image: url('${thumbnailUrl}')"></div>
              <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent"></div>
              <div class="absolute bottom-0 left-0 right-0 p-8">
                <div class="inline-block px-3 py-1 mb-4 text-xs font-bold uppercase tracking-wider bg-primary text-white rounded-full">
                  Episode ${episode.episode_number}
                </div>
                <h2 class="text-3xl md:text-4xl font-bold text-white mb-3 leading-tight">
                  ${episode.title}
                </h2>
                <p class="text-white/90 text-lg mb-4 line-clamp-2">
                  ${episode.header}
                </p>
                <div class="flex items-center gap-4 text-white/80 text-sm">
                  <span>${episode.duration_minutes} min</span>
                  <span>•</span>
                  <span>${new Date(episode.published_at).toLocaleDateString(
                    "en-US",
                    { month: "short", day: "numeric", year: "numeric" }
                  )}</span>
                </div>
              </div>
            </div>
          </a>
        `;
      }

      function renderEpisodeCard(episode) {
        let thumbnailUrl = episode.thumbnail_url;
        if (!thumbnailUrl && episode.youtube_url) {
          const videoId = extractYouTubeID(episode.youtube_url);
          if (videoId) {
            thumbnailUrl = `https://img.youtube.com/vi/${videoId}/hqdefault.jpg`;
          }
        }

        return `
          <a href="talks-detail.html?slug=${episode.slug}" class="group">
            <div class="bg-background-light dark:bg-background-dark rounded-xl overflow-hidden border border-subtle-light/20 dark:border-subtle-dark/20 hover:border-primary/50 transition-all duration-300 h-full flex flex-col">
              <div class="relative overflow-hidden h-48">
                <div class="w-full h-full bg-cover bg-center transition-transform duration-500 group-hover:scale-110" 
                     style="background-image: url('${thumbnailUrl}')"></div>
                <div class="absolute top-3 left-3">
                  <span class="px-3 py-1 bg-primary text-white text-xs font-bold rounded-full">
                    Ep ${episode.episode_number}
                  </span>
                </div>
              </div>
              <div class="p-6 flex-grow flex flex-col">
                <h3 class="text-xl font-bold mb-3 line-clamp-2 group-hover:text-primary transition-colors">
                  ${episode.title}
                </h3>
                <p class="text-subtle-light dark:text-subtle-dark text-sm mb-4 line-clamp-3 flex-grow">
                  ${episode.header}
                </p>
                <div class="flex items-center gap-3 text-xs text-subtle-light dark:text-subtle-dark pt-4 border-t border-subtle-light/20 dark:border-subtle-dark/20">
                  <span>${episode.duration_minutes} min</span>
                  <span>•</span>
                  <span>${new Date(episode.published_at).toLocaleDateString(
                    "en-US",
                    { month: "short", day: "numeric" }
                  )}</span>
                </div>
              </div>
            </div>
          </a>
        `;
      }

      function extractYouTubeID(url) {
        const regExp =
          /^.*((youtu.be\/)|(v\/)|(\/u\/\w\/)|(embed\/)|(watch\?))\??v?=?([^#&?]*).*/;
        const match = url.match(regExp);
        return match && match[7].length == 11 ? match[7] : null;
      }

      function applyFilters() {
        const sortFilter = document.getElementById("sortFilter").value;

        // Sort
        if (sortFilter === "episode") {
          filteredEpisodes.sort((a, b) => b.episode_number - a.episode_number);
        } else if (sortFilter === "duration") {
          filteredEpisodes.sort(
            (a, b) => b.duration_minutes - a.duration_minutes
          );
        } else {
          // Recent (default)
          filteredEpisodes.sort(
            (a, b) => new Date(b.published_at) - new Date(a.published_at)
          );
        }

        renderEpisodes();
      }

      // Event listeners
      document
        .getElementById("sortFilter")
        .addEventListener("change", applyFilters);

      // Load navigation
      fetch("navigation.html")
        .then((res) => res.text())
        .then((html) => {
          document.getElementById("nav-container").innerHTML = html;
          document.body.setAttribute("data-page", "talks-dynamic");
        })
        .catch((err) => console.error("Navigation load error:", err));

      // Load episodes on page load
      document.addEventListener("DOMContentLoaded", loadEpisodes);
    </script>
  </body>
</html>
